[package]
name = "Proxy-CosmodromeRustCore"
version = "0.1.0"
edition = "2024"

[build-dependencies]
swift-bridge-build = "0.1"

[dependencies]
swift-bridge = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"

[lib]
crate-type = ["staticlib"]
